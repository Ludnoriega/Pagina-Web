<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <title>LOGIN</title>
        <link rel="stylesheet" href="resources/css/estilo_login.css">
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Fira+Sans:wght@700&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Fira+Sans&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Fira+Sans:wght@700&display=swap" rel="stylesheet">
    </head>
    <body>
        <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <div class="nombre">
            <h3><img src="resources/css/letra-m.png" height="4%" width="4%" class="img"/>INSTITUTO MANUEL DE FALLA</h3>
        </div>
        <div class="div_titulo">
            <h2 class="titulo">NECESITAMOS QUE INGRESE LOS SIGUIENTES DATOS:</h2>
        </div>
        <form action="/auth" method="POST">
            <div class="input_1">
                <input type="usr" name ="usr"  id="usuario" class="nombre_input" required placeholder="USUARIO">
            </div>
            <div class="input_2">
                <input type="password" name ="password"  id="contrasena" class="contra_input" required placeholder="CONTRASEÑA">
            </div>
            <div class="boton">
                <input type="submit" id="enviarr" class="enviar" value="ENVIAR">
            </div>
            <div class="check">
                <input type="checkbox" name="recordar" id="recordar" class="checkbox">
                <label for="recordar" >Recordar contraseña</label>
            </div>
        </form>

        <% if(typeof alert != "undefined") { %>
            <script>
              Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: '<=% showConfirmButton%>',
                timer: '<%= timer %>'
              }).then(()=>{
                window.location='/<%=ruta %>'
              })
            </script>
        <% } %>
    </body>
</html>